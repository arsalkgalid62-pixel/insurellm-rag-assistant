# Windows/corporate networks often fail npm SSL verify (UNABLE_TO_VERIFY_LEAF_SIGNATURE).
# Matches our Python truststore workaround — local dev only.
strict-ssl=false
fetch-timeout=120000
fetch-retries=5
